Task: Fix the Reports tab so Expense values appear in red.

In JS.html, update the applyReportFilters() function. Currently, expenses are stored in the data as positive numbers, causing the formatMoney() function to incorrectly color them green.

Please update the logic for the Total Expense summary card and the HTML table generation loop. Before passing an amount into formatMoney(), check if the transaction type is "Expense". If it is, explicitly convert the amount to a negative number (e.g., -Math.abs(amount)) so that formatMoney() correctly applies the red styling.
