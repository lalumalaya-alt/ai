Task: Update the Reports tab to show only the Room Rent (excluding Electricity) for Rent Income.

Update Backend (Code.gs):
Locate the getComprehensiveReport() function. Scroll down to the section labeled // 1. Rent Collection.

Inside the rentRows.forEach loop, the code currently pushes the TOTAL_AMOUNT (which includes electricity) to the report. Change it to push the RENT_AMOUNT instead.

Replace this exact line:
amount: Number(row[RENT_COLUMNS.TOTAL_AMOUNT]) || 0,

With this updated line:
amount: Number(row[RENT_COLUMNS.RENT_AMOUNT]) || 0,
