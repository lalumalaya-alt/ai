Task: Change the "Filter by Tenant" control in the Reports tab from a single-select dropdown to a multi-select checkbox list.

Please make the following updates to the frontend files:

1. Update Index.html (HTML Structure):
In the Reports tab's filter section, locate the <select> element used for the Tenant ID dropdown. Replace this entire dropdown element with an empty <div> container and give it a specific ID (for example, repTenantCheckboxes). Do not remove the outer wrapping <div> that controls when the section is shown or hidden. Add some basic styling to this new empty container so that the future checkboxes will display in a neat, scrollable list.

2. Update JS.html (Populating the Checkboxes):
Locate the initialization block at the top of the file that currently fetches the tenants and creates <option> tags. Change this logic so that it loops through the returned tenants and generates an <input type="checkbox"> for each one. Set the value of each checkbox to the Tenant ID, wrap it in a <label> containing the tenant's name, and insert them into your new checkbox container. Ensure each checkbox triggers the report filter function when clicked.

3. Update JS.html (Reading the Checkboxes):
Inside the applyReportFilters function, find the section that handles filtering by specific Tenant IDs for "Rent". Delete the old logic that reads the value of the single dropdown. Replace it with logic that finds all currently checked checkboxes inside your new container and extracts their values into a list (array).

4. Update JS.html (Data Filtering Logic):
Immediately below that, update the array filtering logic. If the user has checked at least one checkbox, filter the report data array so it only keeps rows where the "details" string contains at least one of the selected Tenant IDs from your checked list. If no checkboxes are checked, skip this filter entirely so it defaults to showing all tenants.
