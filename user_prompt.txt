Task: Add an "Export to Excel" button specifically for the F&O Trading reports, which exports the specialized 5-column F&O layout.

Please make the following updates to the frontend:

1. Update Index.html (Add the Button):
Locate the Reports tab where you previously added the "Export Rent as Excel" button. Right next to it, add a second button labeled "Export F&O as Excel". Give this new button a unique ID (like exportFoBtn), hide it by default, and set its click event to trigger a new function called exportFoReport.

2. Update JS.html (Show/Hide Logic):
Inside the applyReportFilters function, find the logic you added earlier that shows or hides the Rent export button based on the selected category. Update this logic to handle both buttons:

If the category is exactly "Rent", show the Rent export button and hide the F&O export button.

If the category is exactly "F&O Trading", show the F&O export button and hide the Rent export button.

If the category is anything else, hide both buttons.

3. Update JS.html (Create the F&O Export Function):
At the bottom of your JavaScript file, create the new exportFoReport function.

Have it verify that currentFilteredReportData contains data (alerting the user if it's empty).

Start building a CSV string with exactly these headers: Date, Broker, Gross PNL, Charges, Net PNL.

Loop through the currentFilteredReportData array. For each row, extract the date, the entity (which holds the Broker name), the gross amount, the charges, and the net amount.

Add each extracted row to your CSV string.

Finally, use the exact same link-clicking logic from the Rent export function to trigger an automatic download, naming the file "FO_Report.csv".
