Task: Update the Universal Reports tab so that when "F&O Trading" is selected, the table changes to a specialized layout showing only Date, Broker, Gross PNL, Charges, and Net PNL.

Please make the following updates to the backend and frontend:

1. Update Backend (Code.gs):
Locate the getComprehensiveReport function. Scroll down to the section for "F&O Income". Currently, the Gross and Charges values are combined into a single text string inside the "details" property. Please update the object being pushed into the array so it also includes two brand-new, separate properties: gross (mapped to TOTAL_GROSS) and charges (mapped to TOTAL_CHARGES). Keep the rest of the object properties exactly the same.

2. Update Frontend Logic (JS.html):
Locate the applyReportFilters function. Scroll down to the section where the HTML table is generated and drawn to the screen.

Wrap the table generation logic in an if/else statement based on the value of the Category dropdown:

If the Category filter is exactly "F&O Trading":
Create a completely custom table layout. The headers should be restricted to exactly 5 columns: "Date", "Broker", "Gross PNL", "Charges", and "Net PNL". Loop through the filtered data and map r.date to Date, r.entity to Broker, r.gross to Gross PNL, r.charges to Charges, and r.amount to Net PNL. Apply your existing formatMoney() function to the Gross, Charges, and Net PNL columns.

If the Category filter is anything else (or "All"):
Render the standard 8-column table with all the generic columns (Type, MOP, SOP, Details) exactly as the code currently does.
