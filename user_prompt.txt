Task: Remove the "Filter & View Expenses" section from the Expenses tab.

1. Update Frontend HTML (Index.html):
Locate the <div id="expenses" class="tab-content"> block. Scroll to the bottom of that section and delete the entire <div class="dash-table-card"> that contains the heading <h3><i class="bi bi-funnel"></i> Filter & View Expenses</h3>.

2. Update Frontend JavaScript (JS.html):

Locate and completely delete the loadExpenseList() function.

Inside the saveExpense() function, look at the google.script.run.withSuccessHandler block and remove the loadExpenseList(); function call so it doesn't try to update a table that no longer exists.
