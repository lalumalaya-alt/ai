Task: Update the Universal Reports so Rent Income only shows Room Rent and can be filtered by Tenant ID.

Update Backend (Code.gs):
Locate the getComprehensiveReport() function and find the section labeled // 1. Rent Collection.

Update the data being pushed for Rent so that the amount is strictly the RENT_AMOUNT (excluding electricity), and append the TENANT_ID into the details string. This will allow the frontend's existing "Search Name/Details" box to automatically filter the report by Tenant ID.

Replace the reportData.push({...}) block for Rent with this exact code:

JavaScript
          reportData.push({
            date: normalizeDateValue(row[RENT_COLUMNS.DATE]),
            type: 'Income',
            category: 'Rent',
            entity: row[RENT_COLUMNS.NAME],
            amount: Number(row[RENT_COLUMNS.RENT_AMOUNT]) || 0, // Room rent only
            mop: row[RENT_COLUMNS.MOP] || "",
            sop: "",
            details: 'Tenant ID: ' + (row[RENT_COLUMNS.TENANT_ID] || "") + ' | Bill ID: ' + (row[RENT_COLUMNS.BILL_ID] || "")
          });
