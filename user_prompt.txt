Task: Update the Universal Reports tab to include a Tenant ID dropdown that only appears when the 'Rent' category is selected, and use it to filter the report data.

Please make the following updates to the frontend files:

1. Update Index.html (HTML Structure):
Inside the Reports tab's filter section, add a new dropdown input labeled "Filter by Tenant". Place it immediately after the existing "Category" dropdown. Set this new dropdown container to be hidden by default.

2. Update JS.html (Dropdown Population):
In the main initialization block that runs when the page loads, add a call to the existing backend function that retrieves all tenants. Use the returned list to populate the new "Filter by Tenant" dropdown with the tenant IDs and names.

3. Update JS.html (Show/Hide UI Logic):
Inside the applyReportFilters function, add a check for the currently selected Category. If the category is exactly "Rent", change the Tenant dropdown container's style to be visible. If any other category is selected, hide the Tenant dropdown container.

4. Update JS.html (Data Filtering Logic):
Further down inside the applyReportFilters function, add a new filtering step. If the category is "Rent" and the user has selected a specific Tenant ID (meaning they didn't select "All"), filter the report data array. Only keep the rows where the "details" text string contains that selected Tenant ID.
