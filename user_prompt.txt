Task: Fix the Reports tab so negative F&O Income (losses) correctly appear in red.

In JS.html, locate the applyReportFilters() function. Scroll down to the bottom where the HTML table is generated inside the filtered.forEach(r => { ... }) loop.

Find this exact line of code:
const displayAmount = r.type === 'Expense' ? -Math.abs(Number(r.amount)) : Math.abs(Number(r.amount));

Replace it with this line:
const displayAmount = r.type === 'Expense' ? -Math.abs(Number(r.amount)) : Number(r.amount);
