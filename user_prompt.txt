Task: Stop the Dashboard "Filter by Month" input from stretching across the full screen.

Update Frontend HTML (Index.html):
Locate the "Filter By Month" section inside the #dashboard tab.

Change the inline style of the container <div> that holds the input and button. Change width: 100%; to max-width: 350px; (or your preferred size).

Update the HTML to look exactly like this:

HTML
<div class="form-group" style="margin: 0;">
  <label for="dashboardMonthFilter"><i class="bi bi-calendar3"></i> Filter By Month</label>
  <div style="display: flex; gap: 10px; max-width: 350px;">
    <input type="month" id="dashboardMonthFilter" onchange="refreshDashboard()" style="flex: 1;">
    <button class="btn btn-secondary" onclick="document.getElementById('dashboardMonthFilter').value=''; refreshDashboard();" style="white-space: nowrap;">All Months</button>
  </div>
</div>
